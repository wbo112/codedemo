server:
  port: 8081

spring:
  application:
    name: cloud-consumerzk-order
  cloud:
    zookeeper:
      #注册到zookeeper  zookeeper IP PORT
      connect-string: 192.168.50.20:2181
logging:
  level:
    root: debug
